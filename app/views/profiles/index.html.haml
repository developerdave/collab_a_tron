%section#profiles
  %ul
    %li
      =link_to image_tag("add_new.png"), new_profile_path, :title => "Add Profile", :id => "new_profile"
    - @profiles.each do |profile|
      %li
        %a{:rel => "tooltip", :title => profile.display_name}
          %img{:src => profile.gravatar_url}